// import { useState } from "react";
import { Board } from "./Board";

const gameStyles = {
  backgroundColor: "salmon",
  margin: 10,
  padding: 20,
};

const Game = () => {
  return (
    <div
      style={{
        backgroundColor: "skyblue",
        margin: 10,
        padding: 20,
      }}>
      Game
      <div style={gameStyles}>Test</div>
    </div>
  );
};

export { Game };
